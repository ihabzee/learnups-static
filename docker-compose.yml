services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-changeme}
      - PORT=8080
    volumes:
      - ./content.json:/app/content.json
    restart: unless-stopped
