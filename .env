# Google Cloud project ID (required for Firestore in production)
# Leave unset to use local content.json file instead
GOOGLE_CLOUD_PROJECT=savvy-celerity-241922

# Admin panel password (change this!)
ADMIN_PASSWORD=Adam101897@

# Flask dev mode (never set in production)
# FLASK_DEBUG=1

# Port (Cloud Run sets this automatically)
PORT=8080
